
RSpec.describe Virtuous::ContactTag do

    # describe "#find_by_contact" do
    #     let(:id) { 33 }
    #     it "returns an array of notes for specified contact" do 
    #         VCR.use_cassette("contact_note") do
    #             @contact_note = Virtuous::ContactTag.find_by_contact(id)
    #         end
    #         expect(@contact_note).to be_a Array
    #     end
    # end     

    # describe "#create" do
    #     let(:body) {{ attributes: [] }}

    #     it "posts the email as a contact note" do
    #         expect {
    #             VCR.use_cassette("contact_note_error") do
    #                 @contact_note = Virtuous::ContactTag.create({})
    #             end
    #         }.to raise_error Virtuous::Error

    #     end

    #     it "returns an Array" do 
    #         VCR.use_cassette("contact_note_error") do
    #             @contact_note = Virtuous::ContactTag.create(attributes)
    #         end
    #         expect(@contact_note).to be_a Array
    #     end
    # end



end
